{
  "name": "todos-monorepo",
  "private": true,
  "scripts": {
    "dev:client": "pnpm --filter todos dev",
    "dev:admin": "pnpm --filter admin dev",
    "build:client": "pnpm --filter todos build",
    "build:admin": "pnpm --filter admin build",
    "dev": "pnpm -r --parallel dev"
  }
}
